# tc-benchtop-settings

System-defaults RPM for **TechniComp Benchtop Linux** (immutable Slowroll-based openSUSE derivative). Built on OBS directly from this repo via scmsync — the config files at the repo root are installed by the spec as Sources (no tarball). Per-file provenance lives in the TC Benchtop design notes.
< ci: webhook smoke test 2026-09-04 -->
