# TechniComp Benchtop Linux: /dev/i2c-* device nodes, which OpenRGB needs for
# SMBus lighting (RAM, some motherboards). openSUSE loads i2c-dev only through
# OpenRGB-systemd-unit, which also installs the OpenRGB app and its service.
i2c-dev
