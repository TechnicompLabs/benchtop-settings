# Technicomp Benchtop Linux: faster shutdown. The user manager, with everything
# still running in the session, is killed if it has not stopped after 15
# seconds; systemd's default for user@.service is 120 seconds.
[Service]
TimeoutStopSec=15s
