# TechniComp Benchtop Linux: faster shutdown of the user session. A user service
# (an application) that has not stopped after 15 seconds is killed; the user
# manager's default is 90 seconds. System services have the same limit
# (system.conf.d/90-tcbl-shutdown.conf).
[Manager]
DefaultTimeoutStopSec=15s
