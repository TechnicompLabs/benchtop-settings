# TechniComp Benchtop Linux: after an automatic update, ask the logged-in users to
# restart (transactional-update-notifier) instead of restarting the machine. With
# the default REBOOT_METHOD=auto, transactional-update.service reboots at once
# unless rebootmgr is running.
REBOOT_METHOD=notify
